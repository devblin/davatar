The `davatar` class is used to generate a unique avatar image based on user input. It uses various helper and validation functions to ensure the image is of the correct size and contains valid text. The `generate` method takes in optional parameters such as size, text, text color, background color, font family, and font weight, and returns a data URL of the generated avatar image.

Parameters:
- `size` (optional): Specifies the size of the avatar image in pixels. Defaults to 64.
- `text` (optional): Specifies the text to be used for the initials on the avatar image. Defaults to an empty string.
- `textColor` (optional): Specifies the color of the text on the avatar image.